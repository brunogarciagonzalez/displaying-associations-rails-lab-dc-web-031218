# lists songs in 'Artist Name - Song Title' format
# links to the artist

<h1>Songs</h1>

<% @songs.each do |song| %>
  <p> <%= link_to "#{song.artist.name} - #{song.title}", song_path(song) %> </p>
<% end %>
